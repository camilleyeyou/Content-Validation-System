[build]
builder = "nixpacks"

[deploy]
startCommand = "cd portal/backend && PYTHONPATH=/app:$PYTHONPATH uvicorn app.main:app --host 0.0.0.0 --port $PORT"

[build]
buildCommand = "pip install -r portal/backend/requirements.txt"