[build]
builder = "nixpacks"
buildCommand = "pip install -r portal/backend/requirements.txt"

[deploy]
startCommand = "cd portal/backend && PYTHONPATH=/app:/app/portal/backend uvicorn app.main:app --host 0.0.0.0 --port $PORT"